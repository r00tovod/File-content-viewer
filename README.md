# File-content-viewer
A simple Python script that reads all files in a directory and prints their contents.

## Features
- Reads all files in a directory
- Prints content to console
- Lightweight and beginner-friendly

## Usage
1. Set the directory path
2. Run the script:
   python script.py directory/
